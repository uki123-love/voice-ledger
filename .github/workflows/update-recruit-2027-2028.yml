name: Update 2027-2028 recruitment workbench
on:
  workflow_dispatch:
  schedule:
    - cron: '15 1,9 * * *'
permissions:
  contents: write
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r recruit-2027-2028/requirements.txt
      - run: python recruit-2027-2028/collector.py
      - name: Commit refreshed data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add recruit-2027-2028/data/jobs.json recruit-2027-2028/data/interviews.json
          git diff --cached --quiet || git commit -m "chore: refresh recruitment data"
          git push
